const { ethers } = require("ethers");
const {userModel} = require('../models/user')
const uploadImageController = async (req, res, next) => {
  try {
   
  
  } catch (error) {
  }
};

module.exports = { uploadImageController }; // single export
